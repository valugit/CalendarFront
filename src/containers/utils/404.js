import React from 'react';

const NotFound = () => <div id={ 'error' } className={ 'b-light-background c-dark-blue' }>
  <h1>404</h1>
  <p>Something's not quite right...</p>
</div>;

export default NotFound;